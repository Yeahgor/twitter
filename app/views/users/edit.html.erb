<% provide(:title, "Edit user") %>
<h1>Захотелось поменять что-то? Начни с себя!</h1>

<div class="row">
  <div class="col-md-5 col-md-offset-2">
    <%= form_for(@user) do |f| %>
      <%= render 'shared/error_messages' %>

      <%= f.label :name, "Ваше имя в проекте"%>
      <%= f.text_field :name, class: 'form-control' %>

    <%= f.label :email, "Контактный e-mail"%>
      <%= f.text_field :email, class: 'form-control' %>

      <%= f.label :password, "Пароль по сложнее чтоб забыть"%>
      <%= f.password_field :password, class: 'form-control' %>

      <%= f.label :password_confirmation, "Тренеруем память, вспоминаем пароль" %>
      <%= f.password_field :password_confirmation, class: 'form-control' %>

      <%= f.submit "Сохранить для потомков", class: "btn btn-primary" %>
    <% end %>

    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <a href="http://gravatar.com/emails"> Поменять аватарку?</a>
    </div>
  </div>
</div>